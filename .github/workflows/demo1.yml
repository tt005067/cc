name: Events Demo 1
on:
  pull_request:
    types:
      - opened # activity type
    branches: # filter
      - main # main
      - 'dev-*' # dev-new dev-this-is-new
      - 'feat/**' # feat/new feat/new/button
  workflow_dispatch: # how add the second event inthis mode
  push:
    branches: # filter
      - main # main
      - 'dev-*' # dev-new dev-this-is-new
      - 'feat/**' # feat/new feat/new/button
    paths-ignore:
      - '.github/workflows/*' # ignore changes in this folder
    # paths:
    # - '.github/workflows/*' # or just in this folder
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output event data
        run: echo "${{ toJSON(github.event) }}"
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Test code
        run: npm run test
      - name: Build code
        run: npm run build
      - name: Deploy project
        run: echo "Deploying..."
# on: [push, workflow_dispatch] : passing multipple events
# activity type
# passing activity type to and event if available! :
# on:
# pull_request:
#  types: [opened,closed] or above
# the next event must be at the same position as the first one  workflow_dispatch:

# activity filters
# ex :on.push.<branches|tags|branches-ignore|tags-ignore>
# = 'dev-*' all dev branches start with dev-
# 'feat/**'
# we can do also based on the path (if it's available for that event )ex: igoner the path to just in the specifi path
